// Using an enum for the names to avoid duplicates
export class AdminRequests {

    public static readonly SUBMITTOPIC: string = "/api/2/createtopic";
    public static readonly GETALLUSERS: string = "/api/2/allusers";
}
