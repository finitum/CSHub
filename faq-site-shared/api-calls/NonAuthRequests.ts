// Using an enum for the names to avoid duplicates
export class NonAuthRequests {

    public static readonly LOGINREQUEST: string = "/api/login";
    public static readonly CREATEACCOUNTREQUEST: string = "/api/createaccount";
    public static readonly GETHOMEDATA: string = "/api/home";
}
