// This will be different on the server, but for testing purposes a public private key is fine :)
// Possible to generate cat /dev/urandom | base64 | head -c 256

export const secretKey: string = "101010";
